# Pinned PEP 517 backend used only while building the project wheel.
# It is installed into a disposable build environment and is not shipped in
# the runtime wheelhouse.

setuptools==83.0.0 \
    --hash=sha256:29b23c360f22f414dc7336bb39178cc7bcbf6021ed2733cde173f09dba19abb3
